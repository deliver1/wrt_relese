# --- 1. 核心硬件适配 (修复内存识别问题) ---
# ⚠️ 必须改！AX6 是 512M 内存，你之前选 256 会导致一半内存无法使用
CONFIG_IPQ_MEM_PROFILE_512=y
# 禁用 256M 配置 (显式关闭以防冲突)
# CONFIG_IPQ_MEM_PROFILE_256 is not set

# --- 2. NSS 硬件加速 (有线性能核心) ---
# 开启 NSS 高性能内存模式 (牺牲少量 RAM 换取极致转发性能)
CONFIG_NSS_MEM_PROFILE_HIGH=y
# 开启 SKB 回收 (降低 CPU 占用，千兆/2.5G 跑满 CPU 不占用的关键)
CONFIG_KERNEL_SKB_RECYCLER=y
CONFIG_KERNEL_SKB_RECYCLER_MULTI_CPU=y
# 启用 NSS 驱动核心
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
# 启用 NSS 版的流控 (比普通 SQM 性能强太多，打游戏必开)
CONFIG_PACKAGE_sqm-scripts-nss=y

# --- 3. 剔除 Wi-Fi 相关 (极致瘦身) ---
# ⚠️ 这一步能省下巨量空间，给 OpenClash 腾地方
# 禁用 Wi-Fi 6 驱动
# CONFIG_PACKAGE_kmod-ath11k-pci is not set
# CONFIG_PACKAGE_ath11k-firmware-ipq8074 is not set
# CONFIG_PACKAGE_ath11k-firmware-qcn9074 is not set
# 禁用无线配置工具和加密库
# CONFIG_PACKAGE_wpad is not set
# CONFIG_PACKAGE_wpad-openssl is not set
# CONFIG_PACKAGE_hostapd is not set
# 禁用之前误选的 Mesh 组件
# CONFIG_NSS_DRV_WIFI_MESH_ENABLE is not set
# CONFIG_PACKAGE_MAC80211_MESH is not set
# CONFIG_ATH11K_NSS_MESH_SUPPORT is not set

# --- 4. 辅助增强 ---
# 建议开启 Shortcut FE (SFE) 作为 NSS 的备选/辅助
CONFIG_PACKAGE_kmod-shortcut-fe=y
CONFIG_PACKAGE_kmod-shortcut-fe-cm=y
