# ==========================================
# NSS 核心加速设置 (AX6 性能核心)
# ==========================================
# 内存配置 (保持默认即可，防报错)
CONFIG_IPQ_MEM_PROFILE_256=y
CONFIG_ATH11K_MEM_PROFILE_512M=y
CONFIG_NSS_MEM_PROFILE_HIGH=y

# 启用 NSS 固件 (必须保留)
CONFIG_NSS_FIRMWARE_VERSION_11_4=y

# 启用 NSS 版本的 SQM 流控 (推荐保留，有线网速更稳)
CONFIG_PACKAGE_sqm-scripts-nss=y

# 启用 SKB 回收 (优化网络小包转发效率)
CONFIG_KERNEL_SKB_RECYCLER=y
CONFIG_KERNEL_SKB_RECYCLER_MULTI_CPU=y

# ==========================================
# ❌ 下面这些 Mesh 功能全部禁用 (有线路由不需要)
# ==========================================
# 禁用 NSS 的 WiFi Mesh 支持
# CONFIG_NSS_DRV_WIFI_MESH_ENABLE is not set
# CONFIG_ATH11K_NSS_MESH_SUPPORT is not set

# 禁用 802.11s Mesh 协议
# CONFIG_PACKAGE_MAC80211_MESH is not set

# 禁用 MCS (主要用于 Mesh 多播，主路由一般用不到)
# CONFIG_PACKAGE_kmod-qca-mcs is not set
