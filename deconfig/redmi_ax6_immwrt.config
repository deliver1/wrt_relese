# ==========================================
# 1. 核心机型与编译设置 (Redmi AX6)
# ==========================================
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq807x=y
CONFIG_TARGET_DEVICE_qualcommax_ipq807x_DEVICE_redmi_ax6=y
CONFIG_TARGET_PER_DEVICE_ROOTFS=y
CONFIG_ATH11K_MEM_PROFILE_512M=y

# 编译优化
CONFIG_DEVEL=y
CONFIG_CCACHE=y
CONFIG_USE_APK=n
CONFIG_BUSYBOX_CUSTOM=y
CONFIG_BUSYBOX_CONFIG_TELNET=y

# ==========================================
# 2. NSS 硬件加速 (根据你的截图整合)
# ==========================================
# 这是 AX6 跑满千兆/2.5G 不发热的关键
CONFIG_NSS_FIRMWARE_VERSION_11_4=y
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_kmod-qca-nss-sfe=y
# 开启 NSS 版本的 SQM (比普通 SQM 性能强10倍)
CONFIG_PACKAGE_sqm-scripts-nss=y
CONFIG_PACKAGE_luci-app-sqm=y

# ==========================================
# 3. 基础网络与 IPv6 (主路由必备)
# ==========================================
CONFIG_IPV6=y
CONFIG_PACKAGE_ipv6helper=y
CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
CONFIG_PACKAGE_kmod-tun=y
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_iptables-nft=y
CONFIG_PACKAGE_ip6tables-nft=y

# 基础工具
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_ca-certificates=y
CONFIG_PACKAGE_ca-bundle=y
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_default-settings-chn=y

# ==========================================
# 4. 科学上网 (Nikki + Mihomo)
# ==========================================
# 移除 Passwall/OpenClash，只保留 Nikki
CONFIG_PACKAGE_luci-app-nikki=y
CONFIG_PACKAGE_mihomo=y
# 确保有 gzip 支持，防止订阅更新失败
CONFIG_PACKAGE_kmod-lib-zlib=y

# ==========================================
# 5. 其他保留功能
# ==========================================
# UPnP (游戏机/BT 下载必备，做主路由建议保留)
CONFIG_PACKAGE_luci-app-upnp=y
# CPU 频率调节 (查看温度)
CONFIG_PACKAGE_luci-app-cpufreq=y

# ==========================================
# 6. 垃圾清理 (明确禁用你列表里的多余项)
# ==========================================
# 禁用 iStore 应用商店 (主路由不需要)
# CONFIG_PACKAGE_luci-app-istorex is not set
# CONFIG_PACKAGE_luci-app-store is not set

# 禁用 EasyTier/Lucky/FRP (你没提远程组网需求)
# CONFIG_PACKAGE_luci-app-easytier is not set
# CONFIG_PACKAGE_luci-app-lucky is not set
# CONFIG_PACKAGE_luci-app-frpc is not set

# 禁用 MosDNS/SmartDNS (Nikki 接管 DNS 即可，多了反而冲突)
# CONFIG_PACKAGE_luci-app-mosdns is not set
# CONFIG_PACKAGE_luci-app-smartdns is not set

# 禁用 应用过滤 (OAF 非常吃 CPU，跑满千兆会卡)
# CONFIG_PACKAGE_luci-app-oaf is not set

# 禁用 KMS (激活 Windows 的)
# CONFIG_PACKAGE_luci-app-vlmcsd is not set

# 禁用 网络唤醒
# CONFIG_PACKAGE_luci-app-wol is not set

# 禁用 之前的 Passwall 配置
# CONFIG_PACKAGE_luci-app-passwall is not set
